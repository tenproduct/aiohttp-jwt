version: "3"
services:
  aiohttp_jwt_3_8:
    build:
      context: .
      args:
        python_version: "3.8"
    volumes:
      - ./aiohttp_jwt:/tenplatform/aiohttp-jwt/aiohttp_jwt
      - ./tests:/tenplatform/aiohttp-jwt/tests
